spring:
  application:
    name: GenWriter
  ai:
    dashscope:
      api-key: sk-670875c6a84a4d84a5f83035be45b86d  # 临时硬编码API密钥
      chat:
        enabled: true
        options:
          model: qwen-plus
          temperature: 0.7
      
genwriter:
  llm:
    default-provider: dashscope
    default-model: qwen-plus
    default-temperature: 0.7
    default-max-tokens: 65535
    prompts:
      writing-system-prompt: |
        你是一位专业的写作助手，擅长根据用户的需求提供高质量的写作建议和内容生成。
        请根据用户的输入，提供有帮助、准确且富有创意的回复。
      document-continuation-prompt: |
        请根据以下文档内容继续写作，保持原有风格和语气：
        已有内容：
        {content}
        请继续：
      document-polish-prompt: |
        请对以下文档进行润色优化，提升表达质量，保持原意不变：
        原文：
        {content}
        润色后：
      knowledge-qa-prompt: |
        基于以下知识库内容回答问题：
        知识库内容：
        {context}
        用户问题：{question}
        请根据知识库内容提供准确、简洁的回答。如果知识库中没有相关信息，请明确说明。
